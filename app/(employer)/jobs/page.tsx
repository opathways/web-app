"use client";

import { Heading, View, Text } from "@aws-amplify/ui-react";

export default function Jobs() {
  return (
    <View padding="1rem">
      <Heading level={1}>Job Listings</Heading>
      <Text>TODO: List all job postings (filter, search, etc.)</Text>
    </View>
  );
}
